import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.bukutamu',
  appName: 'bukutamu',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
